# Copy to .env and adjust. Defaults are the 100%-OSS local path.
WORLD_SEED=42
WORLD_AUTOTICK=true
PRIMARY_MODEL=qwen3:4b
FALLBACK_MODEL=qwen2.5:0.5b
# Optional cloud rung — default OFF; the demo makes a point of it.
ENABLE_CLOUD_FALLBACK=false
