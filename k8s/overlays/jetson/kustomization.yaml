apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Jetson (Orin Nano, arm64) overlay. Same base, same config, same envelope —
# see k8s/setup-jetson.md. Real local inference uses qwen3:1.7b; wire the
# gateway's PRIMARY_BASE_URL to an in-cluster Ollama (k8s/base/ollama.yaml).
resources:
  - ../../base
