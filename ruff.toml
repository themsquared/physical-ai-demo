line-length = 100
target-version = "py312"

[lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]  # long lines OK in prompts/config strings

[lint.per-file-ignores]
"robots/server.py" = ["F811"]  # conditional tool defs per robot type
